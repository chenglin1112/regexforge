# Python / uv build + test cruft
__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/
uv.lock

# Generated by `anna-app dev` / `anna-app apps publish` (handle → minted tool_id)
bundle/anna-tool-ids.js

# OS
.DS_Store

# Anna local identity cache (account/host-specific)
.anna/

# Large demo media — host via GitHub Release / Git LFS, not in git history
regexforge-demo.mov
